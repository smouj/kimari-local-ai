Your production nginx server is returning "502 Bad Gateway" errors intermittently for approximately 30% of requests. The upstream is a Node.js application running on port 3000 on the same machine. Describe a systematic debugging approach: what logs to check, what commands to run, what configurations to inspect, and what common causes could produce this behavior. Include specific nginx directives that might be misconfigured.

A Node.js application running in production is crashing with OOM (Out of Memory) errors. The process runs with a 512MB heap limit via `--max-old-space-size`. You need to diagnose the memory leak. Describe step by step: how to generate a heap snapshot before the crash, how to analyze it with Chrome DevTools, common patterns that cause memory leaks in Node.js, and how to use `process.memoryUsage()` or `clinic.js` for ongoing monitoring. Provide concrete commands.

Your MySQL 8.0 server is throwing "Too many connections" errors. The current `max_connections` is 151. You need to diagnose whether this is a connection leak or legitimate load. Explain: how to check current connection count and who is connected, how to identify sleeping connections that may be leaked, how to check your application's connection pool settings, and recommended configuration changes for both MySQL and the application layer.

Your production web application at `https://app.example.com` is showing SSL certificate errors in some browsers but not others. The certificate was renewed 2 days ago. Some users report "NET::ERR_CERT_DATE_INVALID", others see "ERR_CERT_AUTHORITY_INVALID". Walk through the complete diagnosis: checking the certificate chain, verifying intermediate certificates are installed, checking OCSP stapling, testing with openssl s_client, and ensuring the full certificate chain is served by your web server.

A Docker container running your application keeps restarting with exit code 137. It's managed by docker-compose with `restart: always`. The container runs fine locally but fails in production. Provide a systematic debugging guide: what exit code 137 means, how to inspect container logs with `docker logs`, how to check resource limits, how to access the container's filesystem after it crashes, and how to configure health checks to get better diagnostics.
