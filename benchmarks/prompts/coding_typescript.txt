Create a typed React component `UserProfileCard` that accepts props: `user: { id: number, name: string, avatarUrl?: string, bio?: string, role: 'admin' | 'user' | 'moderator' }`. Use proper TypeScript interfaces, make `avatarUrl` and `bio` optional, show a placeholder avatar when none is provided, and display a role badge with different colors per role.

Write a generic TypeScript function `debounce<T extends (...args: any[]) => any>(fn: T, delay: number): T & { cancel: () => void }` that delays execution until `delay` ms after the last call. It must preserve the original function's return type, support arbitrary arguments, include a `cancel` method to clear pending calls, and properly handle `this` context. Show a usage example with a search input handler.

Create Zod schemas for a registration form with these fields: email (valid email format), password (min 8 chars, must include uppercase, lowercase, number, and special char), username (3-20 alphanumeric chars), age (number 18-120, optional), and agreeToTerms (boolean, must be true). Add a custom error map for Spanish error messages. Include a `RegisterFormData` type inferred from the schema.

Implement a type-safe API client class `ApiClient` in TypeScript that wraps fetch. It should have methods: `get<T>(url: string, params?: Record<string, string>): Promise<T>`, `post<T, B>(url: string, body: B): Promise<T>`, `put<T, B>(url: string, body: B): Promise<T>`, and `delete<T>(url: string): Promise<T>`. Include an `ApiError` class with status, message, and response body. Add automatic JSON parsing, base URL configuration, and request/response interceptors.

Write TypeScript utility types: `DeepPartial<T>` that makes all nested properties optional recursively, `DeepRequired<T>` that makes all nested properties required recursively, and `PickByValue<T, V>` that extracts keys whose values match type V. Include comprehensive test cases using `type Equal<X, Y>` pattern to verify correctness at the type level.
