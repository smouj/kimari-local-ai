Generate a user profile as a JSON object with the following exact structure and fields. The user's name is "María García", she is a software engineer with 5 years of experience specializing in backend development. Include these fields: id (random UUID format string), full_name, email (derived from name, use a plausible domain), age (32), occupation, years_of_experience, skills (array of 6 relevant technical skills), is_active (true), created_at (ISO 8601 date string for today's date), and address object with street, city, country, and postal_code. Respond with ONLY valid JSON, no markdown formatting or explanation.

Create a JSON API response object for a paginated list of products. The response must follow this structure: a top-level object with "status" (string "success"), "data" (object containing "items" array and "pagination" object). Include exactly 3 product items in the array, each with id (integer), name, price (float with 2 decimals), category (string), in_stock (boolean), and rating (float 0-5). The pagination object should have: current_page (1), per_page (10), total_items (47), total_pages (5), and has_next (boolean). Respond with ONLY valid JSON, properly formatted, no markdown code blocks.

Generate a structured JSON error response following RFC 7807 (Problem Details for HTTP APIs) format for the following scenario: a user tried to create an account but the email "john@example.com" is already registered. Include these fields: type (URI string identifying the error type), title (short human-readable summary), status (409), detail (specific explanation of what went wrong), instance (URI of the request that caused the error), and a "errors" array with field-level validation details including field name, message, and code. Respond with ONLY valid JSON, no explanation text.
