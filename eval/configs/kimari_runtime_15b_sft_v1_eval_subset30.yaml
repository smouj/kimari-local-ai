# KimariEval v1 — SFT v1 500-step Adapter vs Baseline Scoring (Subset 30)
# v0.1.76-alpha: private artifact persistence fix before manual review.

base_model: "Qwen/Qwen2.5-1.5B-Instruct"
adapter_repo_private: "Smouj013/kimari-runtime-15b-sft-v1-adapter"
dataset_dir: "eval/kimari_private_v1"

subset_size: 30

temperature: 0.2
max_tokens: 128
top_p: 0.9
seed: 42

comparison_mode: "baseline_vs_adapter_scoring"
score_status: "private_proxy_scoring"

hf_jobs_flavor: "a10g-small"
hf_jobs_timeout_minutes: 30
hf_jobs_docker_image: "ghcr.io/astral-sh/uv:python3.12-bookworm"

# Private artifact persistence requirements.
private_artifact_repo_id: "Smouj013/jobs-artifacts"
private_artifact_bucket_path: "kimari-evals/v0176/subset30"
raw_outputs_private_required: true
raw_outputs_private_filename: "raw_outputs_private.json"
scoring_summary_private_filename: "scoring_summary_private.json"
scoring_summary_public_filename: "scoring_summary_public.json"
manual_review_available_required: true

# Safety flags — ALL must be false/correct.
manual_review_required: true
public_benchmark_allowed: false
raw_outputs_commit_allowed: false
gate_state: "BLOCKED"

version: "0.1.76-alpha"
created_at: "2026-05-14"
notes: |
  Rerun subset30 scoring only after private artifact persistence hardening.
  No training. No public benchmark claims. No raw outputs committed.
  Gate BLOCKED.
