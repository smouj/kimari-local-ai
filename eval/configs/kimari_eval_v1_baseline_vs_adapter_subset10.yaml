# KimariEval v1 — Baseline vs Adapter Evaluation (Subset 10)
# Small subset for pipeline validation before larger runs.

base_model: "Qwen/Qwen2.5-1.5B-Instruct"
adapter_repo_private: "Smouj013/kimari4b-micro-sft-adapter-v0"
dataset_dir: "eval/kimari_private_v1"

# Small subset for first pipeline validation
subset_size: 10

# Generation parameters
temperature: 0.2
max_tokens: 256
top_p: 0.9
seed: 42

# HF Jobs configuration
hf_jobs_flavor: "a10g-small"
hf_jobs_timeout_minutes: 30
hf_jobs_docker_image: "pytorch/pytorch:2.5.1-cuda12.4-cudnn9-devel"

# Stack verified from v0.1.49 micro SFT
stack_verified: true

# Safety flags — ALL must be false/correct
manual_review_required: true
public_benchmark_allowed: false
raw_outputs_commit_allowed: false
gate_state: "BLOCKED"

# Metadata
version: "0.1.54-alpha"
created_at: "2026-05-13"
notes: "Subset10 pipeline validation. If this passes, proceed to subset30. No benchmark claims. No raw outputs committed. Gate BLOCKED."